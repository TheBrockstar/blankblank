import React from 'react';
import { StyledFooter, FooterWrapper } from './Styles';

const Footer = _ => {
  return (
    <StyledFooter>
      <FooterWrapper>
          This is a footer.
      </FooterWrapper>
    </StyledFooter>
  )
}

export default Footer;