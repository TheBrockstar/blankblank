import React from 'react';
import { StyledFooter, FooterWrapper } from './Styles';

const Footer = _ => {
  return (
    <FooterWrapper>
      <StyledFooter>
        This is a footer.
      </StyledFooter>
    </FooterWrapper>
  )
}

export default Footer;